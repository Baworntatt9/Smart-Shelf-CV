# App
APP_NAME="Smart Shelf CV API"
DEBUG=true

# CORS — comma-separated origins allowed to call the API
CORS_ORIGINS="http://localhost:3000"

# Model — path to trained YOLO weights (relative to backend/)
MODEL_WEIGHTS="models/best.pt"
CONF_THRESHOLD=0.5

# Uploads
MAX_UPLOAD_MB=10
