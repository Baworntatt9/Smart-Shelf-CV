# App
APP_NAME="Smart Shelf CV API"
DEBUG=true

# CORS — comma-separated origins allowed to call the API
CORS_ORIGINS="http://localhost:3000"

# Model (Day 2-3) — path to trained YOLO weights
MODEL_WEIGHTS="models/yolov8n.pt"
CONF_THRESHOLD=0.5

# Uploads
MAX_UPLOAD_MB=10
